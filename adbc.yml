adbc:
    version: 1

databases:
    master:
        url: postgres://ant:ant@localhost:5432/empty
        schemas:
            public:
                auth_*: True
                auth_user_user_permissions: False

workflows:
    master-info:
        steps:
            - source: master
              command: info
